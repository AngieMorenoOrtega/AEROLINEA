import React from 'react';

const CompraExitosa = () => {
  // Recupera la información de la compra desde el localStorage o la base de datos
  // Por ejemplo, el número de reserva, código del vuelo, asientos comprados, etc.

  return (
    <div>
      <h2>¡Compra Exitosa!</h2>
      {/* Muestra la información de la compra */}
      {/* ... */}
    </div>
  );
};

export default CompraExitosa;
